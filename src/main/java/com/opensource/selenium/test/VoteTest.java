package com.opensource.selenium.test;

import com.opensource.selenium.base.BaseTest;
import com.opensource.selenium.page.MainPage;

public class VoteTest extends BaseTest {

    MainPage mp = new MainPage(driver);
/*
	@Before
	public void before() {
		
		mp.navigateTo();
		
		mp.goToLoginPage().login(config.getUsername(), config.getPassword());
	}

	@Ignore
	@Test
	public void voteForTheMovieTest(){
		
		mp.searchAMovie(config.getMovieName())
		  .goToMoviePage()
		  .voteForTheMovie(config.getMovieScore());
	}
	
	@After
	public void after() {
		
		mp.logOut();
	}
	*/
}
